// TODO check if customer is logged in before can proceed to transaction
// TODO if customer is not a member, should have option to create account 

import React from 'react'

const LoginForm = () => {
  return (
    <div>LoginForm</div>
  )
}

export default LoginForm